# solid

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run src/index.tsx
```

This project was created using `bun create tui`. [create-tui](https://git.new/create-tui) is the easiest way to get started with OpenTUI.



### Notes
encrypt the private keys
> to do 
accounts module
    private key accounts
    frame
    native keystore?
>/
rpc server
chain settings
left transactions-> incoming and passed
middle -> generic
right side -> list of accounts (tabbed view). accounts tab on top and chains tab on bottom

